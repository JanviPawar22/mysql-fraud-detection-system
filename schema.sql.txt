CREATE DATABASE fraud_detection;
USE fraud_detection;

CREATE TABLE Transactions (
    txn_id INT AUTO_INCREMENT PRIMARY KEY,
    customer_id INT,
    txn_date DATE,
    txn_amount DECIMAL(10,2),
    location VARCHAR(100),
    channel VARCHAR(50)
);

INSERT INTO Transactions (customer_id, txn_date, txn_amount, location, channel) VALUES
(101, '2025-08-01', 1200, 'Delhi', 'Online'),
(102, '2025-08-02', 5000, 'Mumbai', 'POS'),
(103, '2025-08-03', 300, 'Delhi', 'Online');